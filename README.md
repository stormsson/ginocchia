# Ginocchiate counter


1. fare composer install
2. copiare il file `.env.dist` in `.env` con la propria configurazione
3. accedere alla cartella web ed eseguire `php -S localhost:9000`
4. importare nel database il file data/database.sql
5. accedere dal browser a http://localhost:9000
